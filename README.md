# Independent Young Developers

Placeholder repository. It currently holds only this README and GitHub's default `SECURITY.md` template. There is no application code, configuration, or CI.

The previous README body was a one-line webhook test note (`webhook teszt 2.0`). Treat this repo as a stub unless more files are added later.
